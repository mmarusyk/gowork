<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to "#{user.first_name} #{user.last_name}", user %>
  <% if current_user.admin? && !current_user?(user) %>
  | <%= link_to 'Видалити', user, method: :delete, data: { confirm: "Ти впевнений?" } %>
  <% end %> 
</li>